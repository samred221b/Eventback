# Eventopia Organizer Web

This is a standalone web portal for organizers (separate from the mobile app).

## Setup

1. Install dependencies

   npm install

2. Create an env file

   Copy `.env.local.example` to `.env.local` and fill in the values.

3. Run

   npm run dev

Then open http://localhost:3000
